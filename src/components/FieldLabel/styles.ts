import { CSSObject } from '@emotion/core';
import { colors } from '@src/colors/colors';

export const base: CSSObject = {
    display: 'block',
    color: colors.primary,
    fontSize: '16px',
    marginBottom: '8px',
    fontWeight: 700
};

export const helptip: CSSObject = {
    marginLeft: '4px'
};

