import { CSSObject } from '@emotion/serialize';
import { colors } from '@src/colors/colors';

export const base: CSSObject = {

};

export const baseSeparator: CSSObject = {
    borderBottom: `2px solid ${colors.silver2}`
};
