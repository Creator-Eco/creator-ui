import { CSSObject } from '@emotion/serialize';

export const base: CSSObject = {

};

export const popoverContent: CSSObject = {
    minWidth: '180px'
};

export const trigger: CSSObject = {
    fontSize: '35px',
    cursor: 'pointer'
};

